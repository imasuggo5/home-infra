---
# Setup Kubernetes
- hosts: microk8s

  roles:
    - microk8s

# Setup servers:
- hosts: master

  roles:
    - docker
    - nfs-server

# Setup Kubernetes infrastructures
- hosts: master

  roles:
    - network
    - nfs-driver
    - kube-prometheus-stack
    - dashboard
